# Take a string and print the letters in decreasing order of frequency.
def most_frequent(string):

    #create empty dictionary and a list
    strdict1 = {}
    strdict2 = {}
    vlist = []
    for let in string:
        lcount = string.count(let)
        strdict1[let] = str(lcount)  #append the letter and its frequency to the dictionary
        strdict2[str(lcount)] = let  #picks up 'i' both the times so 's' doesn't get added


    #for i in sorted(strdict.keys(),reverse = True): #while using function 'sorted' (here) we loose n-1 letters that have the same frequency
        #print(strdict[i] + '=' + i )

    for i in strdict1:
        vlist.append(strdict1[i])

    vlist.sort(reverse = True)

    print(strdict1)
    print(strdict2)

    for val in vlist:
        print(strdict2[val] + '=' + val)





most_frequent('Mississippi')
 # here the letter 's' is lost in both the methods i tried ):
